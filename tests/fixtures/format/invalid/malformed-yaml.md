---
github_url: https://github.com/broken/malformed
full_name: [unterminated flow sequence
category: {also broken
---

# malformed-yaml

The frontmatter block is properly delimited, but its contents are not valid
YAML (unterminated `[` flow sequence and `{` flow mapping). The split
succeeds; the YAML parse must fail.
