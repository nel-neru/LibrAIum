{
  "name": "libraium",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "LibrAIum — a personal, local-first library of best-practice GitHub repositories, with an MCP server for Claude Code.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test tests/*.test.mjs",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-opener": "^2.0.0",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "svelte": "^5.57.0",
    "vite": "^8.0.0"
  }
}
